const TimeSlot = require('../models/TimeSlot');


exports.getAvailableTimeSlots = async (req, res) => {
    const { date } = req.params;

    try {
        const timeSlots = await timeSlots.fin
    } catch (error) {

    }
}